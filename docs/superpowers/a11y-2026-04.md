# A11y Audit — 2026-04

**Status:** Live audit deferred. This document records the audit *framework*
that landed as part of T-061/T-063 and the structural items deferred to
Phase 2; a real walk-through with axe DevTools and screen-reader testing is
scheduled to happen during beta self-testing, after which findings will be
appended below.

## Tooling in place

- `axe-core` ^4 in devDependencies (`apps/desktop/package.json`).
- Test helper at `apps/desktop/src/test/axe.ts` exports `checkA11y(container)`
  and `expectNoA11yViolations(results)`. `RULE_OVERRIDES` is empty until the
  live audit produces a justification for any disabled rule.
- Component matrix tests (`src/__tests__/MATRIX.md`) wrap rendered DOM with
  axe assertions on each matrix-listed surface so regressions are caught at
  unit-test time.

## Findings (live audit pending)

| # | Surface | Finding | Severity | Status | Ticket |
|---|---|---|---|---|---|
| (none yet) | | | | | |

When findings land, append rows here. Use one row per (surface, rule) pair so
that "fixed" rows can be marked ✅ without re-jigging the table.

## Disabled axe rules

| Rule ID | Reason | Audit row |
|---|---|---|
| (none) | | |

Any rule disabled in `apps/desktop/src/test/axe.ts` `RULE_OVERRIDES` MUST cite
a row in the table above. No silent overrides.

## Deferred to Phase 2

The Phase 1 design spec
(`docs/superpowers/specs/2026-04-26-p2-testing-and-polish-design.md` §3)
marked structural a11y rework out of scope for Phase 1. Tracked as separate
issues:

- **#126** — Replace custom widgets with native elements (`<div role="button">`
  → `<button>`, etc.).
- **#127** — `aria-live` announcement for streaming chat tokens.
- **#128** — Full keyboard navigation map for power users (global shortcuts,
  focus discipline, sequential card navigation).

Leaf fixes (`aria-label` on icon-only buttons, `:focus-visible` rings,
contrast token sweep, reduced-motion auto-scroll, onboarding card tab order)
are scheduled for follow-up commits after the live audit produces a concrete
findings list.
